"""模块三：安全审计 + 单机任务调度与生命周期。

避免 `import module3` 时加载 FastAPI。请显式导入，例如：
  from module3.audit.middleware import AuditMiddleware
  from module3.audit.middleware_fastapi import AuditMiddlewareFastAPI
  from module3.scheduler import get_scheduler_service
"""

__all__: list = []
